import { Contender } from '@/app/types';

const contenderDefaults = {
  id: 'contender-0',
  no: 0,
  flag: 'xx',
  name: 'TBD',
  scores: [0, 0, 0],
} satisfies Contender;

export default contenderDefaults;
